title: Piyush Writes
description: AI · ML · Engineering · Python · Tutorials & Experiments

baseurl: ""                    # Important for user site (username.github.io)
url: "https://piyushwrites.github.io"

# Keep your existing theme/custom styles
# theme: minima               # optional - comment out if you want to keep pure custom HTML
# We will keep your current index.html as homepage

# Future plugins (add later if needed)
plugins:
  - jekyll-feed             # for RSS

# Optional: better permalinks
permalink: /:title/

# Optional: timezone for dates
timezone: Asia/Kolkata
